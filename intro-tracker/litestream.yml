dbs:
  - path: /data/crm.db
    replicas:
      - type: s3
        bucket: ${BUCKET_NAME}
        path: crm.db
        endpoint: ${AWS_ENDPOINT_URL_S3}
        region: ${AWS_REGION}
        force-path-style: true
